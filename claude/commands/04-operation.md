---
description: "AI-PLC Stage 4: Operation — タスク実行・成果物生成"
argument-hint: Task ID or Layer path
allowed-tools: Read, Write, Bash, Glob, Grep, WebFetch, WebSearch
---

Read the skill at `.claude/skills/ai-plc/04-operation/SKILL.md` and execute the Operation stage.

Target: $ARGUMENTS

Requires: Backlog must exist with executable tasks (dependencies resolved, status pending).
