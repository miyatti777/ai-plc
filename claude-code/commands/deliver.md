---
description: Start AIPO Deliver phase — execute tasks and produce deliverables
argument-hint: Task ID or Layer path
allowed-tools: Read, Write, Bash, Glob, Grep, WebFetch, WebSearch
---

Read the skill at `.claude/skills/aipo/04-deliver/SKILL.md` and execute the Deliver phase.

Target: $ARGUMENTS

Requires: Focus phase must be completed (tasks.yaml must exist with pending tasks).
